name: CI

on: push

jobs:
  test:
    runs-on: ubuntu-latest

    # strategy:
    #   matrix:
    #     target: [test-clj]
    
    steps:
    - name: Setup CI Environment
      uses: yetanalytics/actions/setup-env@v0.0.2
    
    - name: Run Makefile target test-clj
      run: clojure -X:test:run-clj
